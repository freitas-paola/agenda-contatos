import { SideBarContainer } from './styles'
import { Form } from '../../components/Form'

export const Sidebar = () => {
  return (
    <SideBarContainer>
      <Form />
    </SideBarContainer>
  )
}
